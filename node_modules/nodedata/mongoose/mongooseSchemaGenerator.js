"use strict";
const Mongoose = require('mongoose');
const elasticSearchUtils_1 = require("../search/elasticSearchUtils");
var MongooseSchema = Mongoose.Schema;
class MongooseSchemaGenrator {
    createSchema(parsedSchema, mongooseOptions) {
        var mongooseSchemaObj = new MongooseSchema(parsedSchema, mongooseOptions.options);
        if (mongooseOptions.searchIndex) {
            elasticSearchUtils_1.searchUtils.insertMongoosasticToSchema(mongooseSchemaObj);
        }
        return mongooseSchemaObj;
    }
}
exports.schemaGenerator = new MongooseSchemaGenrator();

//# sourceMappingURL=mongooseSchemaGenerator.js.map
